# Fix-bot lessons learned

This file is the bot's cross-issue memory: patterns observed across
multiple maintainer rejections. It's loaded by Agent A's prompt on
every run, near the top of the instructions.

**Maintained by `dead-code-watcher:compact` (the monthly compactor)
running on the same workflow.** Claude reads the skip-list, finds
recurring rejection patterns, and rewrites this file holistically
each month — pruning lessons that no longer apply, adding new ones
that show up repeatedly.

The lessons live in the body below this header. Empty until the
compactor's first run accumulates enough data.

---

(no lessons yet — bot is in its first month of operation)
