node_modules/
dist/
admin-dist/
*.tsbuildinfo
.tmp/
.wrangler/
.seed-output/
.env
.env.*
.claude/settings.local.json
.claude/scheduled_tasks.lock
CLAUDE.local.md
test-results/
.stryker-tmp/
reports/

# Sidecar files generated by `gazetta dev` and the admin's source-side
# sidecar writer when the Publish panel runs. Shape: `.<8-hex>.hash`,
# `.uses-*`, `.tpl-*` (see docs/sidecars.md). Transient — regenerated
# on every run, never authoritative. Anchored under any `targets/` dir
# so we don't accidentally swallow legitimate dotfiles elsewhere.
**/targets/**/.[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f].hash
**/targets/**/.uses-*
**/targets/**/.tpl-*

# Asset-refs reverse-dep index — derived from page/fragment manifests,
# rebuilt on save / publish / reindex CLI. Lives at .gazetta/asset-refs/
# (alongside history). Targets carry their own copy, but we don't commit
# the source-side editable target's copy.
**/targets/**/.gazetta/asset-refs/
