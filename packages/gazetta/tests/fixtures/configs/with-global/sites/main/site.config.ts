import { defineSite } from '../../../../../../src/config/define.js'

export default defineSite({
  name: 'main',
  // No admin.cache or admin.audit set — these should inherit from gazetta.config.defaults
})
